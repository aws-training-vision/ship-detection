the plan:
data pre-processing:
  -???

training:
  -load data from s3
  -load model x???
  -train model x??? with training data subset
  -validate against validation data subset

test:
  -run against unlabeled test data.
